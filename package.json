{
  "name": "lovec-vltavinu-reborn",
  "version": "5.4.2",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node tools/validate.mjs",
    "test:browser": "playwright test",
    "test:smoke": "playwright test tests/game-smoke.spec.mjs",
    "test:offline": "playwright test --project=offline-chromium",
    "test:visual": "playwright test tests/visual-regression.spec.mjs",
    "build:publish": "node tools/build-publish.mjs",
    "validate:publish": "node tools/validate-publish.mjs",
    "test:publish": "npm run build:publish && npm run validate:publish && PLAYWRIGHT_ROOT=dist PLAYWRIGHT_BASE_PATH=/Staraverze/ PLAYWRIGHT_PORT=4191 playwright test --project=publish-chromium && PLAYWRIGHT_ROOT=dist PLAYWRIGHT_BASE_PATH=/Staraverze/ PLAYWRIGHT_PORT=4192 playwright test --project=publish-offline-chromium",
    "test": "npm run validate && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
